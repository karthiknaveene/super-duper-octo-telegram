name: tag

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - "*"
jobs:
  run-scheduled-task:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: register-artifact-step-with-options
        uses: cloudbees-io-gha/label-artifact-version@v1
        with:
          cloudbees-url: "https://api.saas-preprod.beescloud.com"
          artifact-id: test
          labels: "For Testing GHA integration"

  run-scheduled-task-1:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        
      - name: Run some scheduled task
        run: |
          echo "This is a scheduled run every 5 minutes."
          # Your actual commands can go here, like tests, builds, etc.
